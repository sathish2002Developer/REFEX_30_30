/**
 * Demo CMS credentials — keep in sync with backend/data/cmsAdminSample.json.
 * After pulling the repo, run from backend: npm run seed:cms-admin
 */
export const CMS_ADMIN_SAMPLE = {
  email: "cms.admin@refex.local",
  password: "RefexCmsDev2026!",
} as const;
